# requirements.md テンプレート

`<workdir>/requirements.md` の構成テンプレート。EARS 記法は `../references/ears-format.md` を参照。

```markdown
# <作業単位名> — 要件

## はじめに

[この作業の概要、背景、解決する課題を 2〜4 文で]

## スコープ

### 対象(やること)

- ...

### 対象外(やらないこと)

- <やらないこと> — 理由: <なぜスコープ外か>

## 前提(未検証の賭け)

要件が依存する、まだ検証していない前提を明示し、各前提に検証方法を添える。検証で覆れば要件に波及する。ユーザーが「お任せ」とした項目の採用案と理由もここに記す。

- <真と賭けている前提> — 検証方法: <どう確かめるか> / 状態: <未検証 / 検証済み>

## 用語定義

| 用語 | 定義 |
| ---- | ---- |
| ...  | ...  |

## 要件

### Requirement 1: <要件タイトル>

**ユーザーストーリー**: <ロール> として、<実現したいこと> したい。なぜなら <理由・価値> だからである。

**受け入れ基準**:
1.1. システムは、〜しなければならない。(常時)
1.2. <イベント> のとき、システムは 〜しなければならない。(イベント)
1.3. <条件> の場合、システムは 〜しなければならない。(異常系)

### Requirement 2: <要件タイトル>

**ユーザーストーリー**: ...

**受け入れ基準**:
2.1. ...
2.2. ...

## 非機能要件

(必要な場合のみ。性能・セキュリティ・可用性・互換性など。これらも EARS で記述し、メトリクス・閾値を明示する)

### Requirement N: <非機能要件タイトル>

**受け入れ基準**:
N.1. ...
```

## 記述ルール(要約)

- 要件見出しは数値 ID のみ(`Requirement 1`)。受け入れ基準は `<要件番号>.<連番>`。
- 受け入れ基準は EARS の 5 パターンで、1 文 1 振る舞い、測定可能に書く。
- 異常系・エッジケースを必ず含める。
- 認証を持つ機能は、ログイン正常系・リロード後のセッション維持・セッション失効時の挙動を受け入れ基準に含める。
- GUI を持つ機能は、共通レイアウトの適用・指定デザイントークンへの準拠を受け入れ基準に含める(視覚設計の正本があれば所在を参照で示す)。
- 不明点は推測せず、生成フェーズでその場で全て質問して解消する。
